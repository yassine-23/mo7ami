# Mo7ami Production Environment Template
# Copy this to .env and fill in with REAL values from your secret manager
# NEVER commit the actual .env file to version control

# ============================================
# App Configuration
# ============================================
NEXT_PUBLIC_APP_URL=https://mo7ami.ma
NEXT_PUBLIC_API_URL=https://api.mo7ami.ma

# ============================================
# OpenAI API (BACKEND ONLY - DO NOT EXPOSE)
# ============================================
OPENAI_API_KEY=sk-proj-...
# NEVER use NEXT_PUBLIC_OPENAI_API_KEY - it exposes keys to browsers

# ============================================
# Authentication (Google OAuth)
# ============================================
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
NEXTAUTH_URL=https://mo7ami.ma

# ============================================
# Supabase (Database + Storage)
# ============================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# ============================================
# Database (Direct Connection)
# ============================================
DATABASE_URL=postgresql://postgres:password@host:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres:password@host:5432/postgres

# ============================================
# Security Note
# ============================================
# After copying this template:
# 1. Rotate ALL credentials (assume current ones are compromised)
# 2. Store secrets in your hosting platform's secret manager
# 3. Add .env to .gitignore
# 4. Never use NEXT_PUBLIC_* for sensitive values
